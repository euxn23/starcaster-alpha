<%= form_for @user, :html => { :class => "form-horizontal user" } do |f| %>

    <% if @user.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :tw_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tw_name, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:tw_name]) %>
  </div>
  <div class="control-group">
    <%= f.label :tw_sid, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tw_sid, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:tw_sid]) %>
  </div>
  <div class="control-group">
    <%= f.label :tw_uid, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tw_uid, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:tw_uid]) %>
  </div>
  <div class="control-group">
    <%= f.label :tw_key, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tw_key, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:tw_key]) %>
  </div>
  <div class="control-group">
    <%= f.label :tw_secret, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :tw_secret, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:tw_secret]) %>
  </div>
  <div class="control-group">
    <%= f.label :provider, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :provider, :class => 'form-control' %>
    </div>
    <%= error_span(@user[:provider]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            users_path, :class => 'btn btn-default' %>

<% end %>
