<div class="well sidebar-nav text-center">
  <%= image_tag user.image, class: 'user-icon' %>
  <h3><%= user.name %></h3>
  <% if user_signed_in? && user.id == current_user.id %>
      <p><%= link_to 'お気に入りを更新', '/users/fetch', remote: true, class:"btn btn-primary btn-lg" %></p>
      <p><a href="/users/setting"><i class="fa fa-cog fa-3x"></i></a><a href="/"><i class="fa fa-refresh fa-3x"></i></a></p>
<% end %>
</div><!--/.well -->